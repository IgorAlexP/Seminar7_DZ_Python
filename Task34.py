"""
Задача 34: Винни-Пух попросил Вас посмотреть, есть ли в его стихах ритм. Поскольку разобраться в его 
кричалках не настолько просто, насколько легко он их придумывает, Вам стоит написать программу. 
Винни-Пух считает, что ритм есть, если число слогов (т.е. число гласных букв) в каждой фразе 
стихотворения одинаковое. Фраза может состоять из одного слова, если во фразе несколько слов, 
то они разделяются дефисами. Фразы отделяются друг от друга пробелами. Стихотворение Винни-Пух 
вбивает в программу с клавиатуры. В ответе напишите “Парам пам-пам”, если с ритмом все в порядке
и “Пам парам”, если с ритмом все не в порядке

Ввод:
пара-ра-рам рам-пам-папам па-ра-па-дам

Вывод:
Парам пам-пам
"""

def count_vowels(word):
    vowels = "aeiouAEIOU"
    count = 0
    for char in word:
        if char in vowels:
            count += 1
    return count

def check_rhythm(poem):
    lines = poem.split()
    syllables = [count_vowels(line.replace('-', '')) for line in lines]
    return "Парам пам-пам" if len(set(syllables)) == 1 else "Пам парам"

poem = input()
print(check_rhythm(poem))

""" 
Сначала определяем функцию count_vowels(word), которая принимает слово и возвращает количество 
гласных букв в нем. Затем определяем функцию check_rhythm(poem), которая принимает стихотворение 
в виде строки poem, разбивает его на строки (фразы) с помощью метода split(), вычисляет число слогов
в каждой строке с помощью count_vowels() и сохраняет результаты в список syllables. Если все элементы 
списка syllables одинаковы (то есть каждая фраза имеет одинаковое количество слогов),
то возвращаем "Парам пам-пам", иначе возвращаем "Пам парам".

Далее считываем стихотворение с помощью функции input() и вызываем функцию check_rhythm() для проверки ритма.
Результат выводится на экран.
"""